const TaskReducer = (state, action) => {
  switch (action.type) {
    case 'EJEMPLO':
      return {
        ...state,
      }
    default:
      return {
        ...state,
      }
  }
}

export default TaskReducer
